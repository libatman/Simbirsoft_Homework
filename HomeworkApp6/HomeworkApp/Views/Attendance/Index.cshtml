@model IEnumerable<HomeworkApp.Models.AttendanceModels>

@{
    ViewBag.Title = "Attendance";
}

<h2>Attendance</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table style="width:100%;">
    <tr>
        <th style="width:20%;">
            @Html.DisplayNameFor(model => model.NumberStudents)
        </th>
        <th style="width:20%;">
        @Html.DisplayNameFor(model => model.IdLection)
</th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumberStudents)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ThemeLection)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
